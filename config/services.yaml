# pacobrnt/actualites/actualites-8503aa1c89f1e8ef1ea8610c253c5ddbee3bdf8d/config/services.yaml

# ... (Début du fichier) ...
parameters:
    # 🚨 AJOUT DES PARAMÈTRES POUR EXPOSER LES VARIABLES D'ENVIRONNEMENT
    gnews_api_key: '%env(GNEWS_API_KEY)%'
    gnews_base_url: '%env(GNEWS_BASE_URL)%'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true
        autoconfigure: true

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'

    # 🚨 CONFIGURATION EXPLICITE POUR NewsApiService (Résout l'erreur)
    App\Service\NewsApiService:
        arguments:
            # $gnewsApiKey (Argument 2) reçoit la valeur du paramètre
            $gnewsApiKey: '%gnews_api_key%'
            # $gnewsBaseUrl (Argument 3) reçoit la valeur du paramètre
            $gnewsBaseUrl: '%gnews_base_url%'

# ... (Fin du fichier) ...
