{# templates/home/index.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Actualités du Moment (GNews){% endblock %}

{% block body %}
    <h1 style="margin-bottom: 25px;">Actualités du Moment</h1>

    {% if articles is empty %}
        <p style="padding: 20px; border: 1px dashed red; color: red;">
            Aucun article trouvé. Veuillez vérifier votre clé API GNEWS_API_KEY et sa configuration.
        </p>
    {% else %}
        {# Grille de cartes pour les articles #}
        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 30px;">
            {% for article in articles %}

                {# Le hash est utilisé pour créer des IDs uniques pour le Turbo Frame et le CSRF #}
                {% set urlHash = article.url|hash %}

                {# Style général de la carte #}
                <div class="article-card" id="article_card_{{ urlHash }}">

                    {# Lien englobant la carte entière (sauf l'espace du bouton) #}
                    <a href="{{ article.url }}" target="_blank" title="{{ article.title }}"
                       style="display: block; text-decoration: none; color: inherit;">

                        {# Zone Image #}
                        <div>
                            {% if article.image %}
                                {# GNews utilise 'image' pour l'URL de l'image #}
                                <img src="{{ article.image }}" alt="{{ article.title }}" style="width: 100%; height: 200px; object-fit: cover;">
                            {% else %}
                                {# Image de secours si l'image est manquante #}
                                <div style="width: 100%; height: 200px; background-color: #f0f0f0; display: flex; align-items: center; justify-content: center; color: #666;">
                                    <span>Image non disponible</span>
                                </div>
                            {% endif %}
                        </div>

                        <div style="padding: 15px;">
                            {# Titre (Maintenant en noir) #}
                            <h2 style="font-size: 1.2em; margin-top: 0; margin-bottom: 10px; color: #343a40;">
                                {{ article.title }}
                            </h2>

                            {# Description (truncation pour la carte) #}
                            <p style="font-size: 0.9em; color: #555; margin-bottom: 15px;">
                                {{ article.description|slice(0, 100) ~ '...' }}
                            </p>

                            <small style="display: block; color: #888;">
                                Source: {{ article.source.name }}
                            </small>
                        </div>
                    </a>

                    {# NOUVEAU: Inclusion du bouton J'aime - EN DEHORS de la balise <a> #}
                    <div style="padding: 0 15px 15px 15px;">
                        {% include 'favorite/_like_button.html.twig' with {
                            'article': article
                        } %}
                    </div>
                </div>
            {% endfor %}
        </div>
    {% endif %}
{% endblock %}
